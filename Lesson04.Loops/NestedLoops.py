names = [['Jobs', 'Gates'], ['Newton', 'Euclid'], ['Einstein', 'Feynman']]

# this outer loop will iterate over each list in the names list
for name in names:
    # this inner loop will go through each name in the list
    for n in name:
        print(n)